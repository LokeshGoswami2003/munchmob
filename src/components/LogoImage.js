import logo from "../assets/logo1.png";

const LogoImage = () => {
    return (
        <div>
            <img src={logo} alt="logo image" className="h-24" />
        </div>
    );
};

export default LogoImage;
